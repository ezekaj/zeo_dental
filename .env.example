# Shared
GEMINI_API_KEY=your_gemini_api_key_here

# Frontend
VITE_ELFSIGHT_WIDGET_ID=your_elfsight_widget_id_here

# Server
DATABASE_URL=postgresql://user:password@host:5432/dbname
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info
NODE_ENV=production
ADMIN_TOKEN=change_me_admin_bearer

# CORS (comma-separated origins). If unset, dev is permissive.
CORS_ORIGINS=https://zeodentalclinic.com,https://www.zeodentalclinic.com
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
# CORS (comma-separated origins). If unset, dev is permissive.
CORS_ORIGINS=https://zeodentalclinic.com,https://www.zeodentalclinic.com

# Shared
GEMINI_API_KEY=your_gemini_api_key_here

# Server
DATABASE_URL=postgresql://user:password@host:5432/dbname
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info
NODE_ENV=production
ADMIN_TOKEN=change_me_admin_bearer

# CORS (comma-separated origins). If unset, dev is permissive.
CORS_ORIGINS=https://zeodental.com,https://zeo-dental.fly.dev
